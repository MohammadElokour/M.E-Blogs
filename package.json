{
  "name": "blogs",
  "version": "1.0.0",
  "description": "user blogs",
  "main": "server.js",
  "scripts": {
    "start": "nodemon server/server.js --ignore public",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "m.elokour",
  "license": "ISC",
  "dependencies": {
    "connect-livereload": "^0.6.1",
    "express": "^4.17.1",
    "livereload": "^0.9.1"
  }
}
